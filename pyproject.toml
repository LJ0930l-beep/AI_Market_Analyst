[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-market-analyst"
version = "1.2.0"
description = "Local-first crypto market monitoring, news translation, Ollama analysis, and paper tracking"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
api = ["fastapi>=0.115", "uvicorn[standard]>=0.30", "httpx>=0.27", "websocket-client>=1.8"]
market = ["yfinance>=0.2.40"]
dev = ["pytest>=8.0"]
desktop = ["pyinstaller>=6.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["apps*", "core*"]
exclude = ["data*", "docs*", "tests*", "scripts*"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 120
