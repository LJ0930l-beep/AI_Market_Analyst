# AI Market Analyst V2R1 真实交易链路修复实施方案

版本：v1.2 · 日期：2026-09-08 · 状态：待开发、待验收

## 1. 目的与实施边界

本方案是 repair-spec-v1.1.md 的修复补充，不替换原有六策略、新闻可信度、国际化和桌面交付要求。针对最新代码审阅发现，将“模块存在”提升为“用户从界面实际操作后端，产生可核验结果”。

保留三种产品体验：交易员辅助、自主模拟、自主实盘。自主模式允许千问9B在一次明确授权范围内自行决定开仓、持有、减仓、平仓和收紧止损，不要求逐笔人工确认，也不要求 AI_LED 必须先收到固定策略信号。AI不能修改授权、提高风险上限或绕过执行网关。

本文件只授权范围内的方案设计；不是交易账户授权、资金使用授权或实盘解锁指令。开发默认 PAPER，生产资金继续锁定。真实 TESTNET 私有操作也应在指定测试账户和明确授权范围内开展。

不以增加新页面、大规模换框架或重写整个项目作为本轮目标。先闭合已有功能，保留现有未提交改动；新增模块必须接入实际入口，禁止另建并行的“演示交易系统”。

## 2. 审阅基线与事实分类

基线目录：D:\RJ\codex\ai-market-analyst。代码含大量未提交修改和新增文件；开发开始时记录 HEAD、dirty diff 摘要及关键文件哈希，不得仅以提交号标识版本。

已执行五组相关后端测试，共33项通过；这只证明这些测试通过，不等价于完整发布验收。独立临时数据库复现确认：部分止盈后剩余仓位不再止损，退出手续费重复扣除。未进行实盘交易、真实测试网成交或本轮全量桌面安装验收。

| 编号 | 已确认问题 | 影响 | 优先级 |
|---|---|---|---|
| F01 | Guardian仅查OPEN，部分退出改成PARTIALLY_CLOSED | 剩余仓位脱离保护 | P0 |
| F02 | 暂停策略同时停止行情流 | Guardian线程存活但没有新价格 | P0 |
| F03 | 无交易客户端生成ACK；PAPER缺价格使用68000 | 假回执、假成交、资产价格错用 | P0 |
| F04 | AI_LED引擎未发现生产实例化/调度；面板为静态周期数据 | 用户看到的AI盯盘并非真实周期 | P1 |
| F05 | 授权检查不完整，网关没有统一硬风控入口 | 直接下单路径绕过额度和组合约束 | P0 |
| F06 | 净盈亏入账后再次单列扣费 | 权益、绩效和日亏损计算错误 | P1 |
| F07 | manifest默认AT01—40通过，TESTNET能力为固定值 | 无法凭证据判断真实完成程度 | P1 |

主要位置：core/trading/position_guardian.py、core/monitoring_runtime.py、core/trading/execution_gateway.py、core/trading/authorization.py、core/trading/ai_led_engine.py、core/trading/ledger.py、core/trading/testnet_capabilities.py、core/manifest.py、apps/api/v2.py、web/src/components/AITraderPanel.tsx。

补充核对：当前AI_LED代码还存在固定PAPER模式、固定数量/价格小数位、提前ACTIVE保护状态、自行登记成交的路径。本轮必须一起消除，不把它们当作完成运行接线后的另一次增量需求。

## 3. 目标闭环与唯一事实来源

完整链路：授权会话 → 行情/持仓/新闻快照 → 固定策略或AI_LED决策 → 结构验证 → 授权验证 → 统一风控与预算预占 → 执行网关 → 场所/模拟适配器 → 成交对账 → 账本和持仓 → Guardian → 界面和研究评估。

职责边界：

- DecisionEngine只产出提案，不直接写成交、不直接连接交易所。
- RiskEngine对所有开仓路径执行同一套硬限制，计算允许数量。
- ExecutionGateway负责持久化委托、幂等、路由和提交状态，不伪造成交。
- Reconciler处理成交事实及未知委托；账本写入采用唯一事件键。
- PositionGuardian独立于AI与策略扫描存活，持续保护剩余数量。
- 前端只呈现事实状态，不自行生成决策、成交价格或保护成功。
- 账户、订单、持仓均显式携带account_id、venue、environment，不按账户名字猜模式。

## 4. 工作包A：先修持仓保护与退出账本

### A1. 保护所有剩余仓位

统一仓位状态契约：OPEN、PARTIALLY_CLOSED、CLOSING、CLOSED；另设独立protection_status，不能用交易状态代替保护状态。查询以剩余数量大于零且未终结为核心，覆盖部分平仓和正在平仓但未完全成交的仓位。

TP1仅按策略目标比例减少实际成交数量；剩余仓位继续被止损/TP2/追踪规则监控。禁止任意固定50%或自动保本覆盖原策略合同。剩余数量、已执行目标、持仓版本及退出委托应持久化；重启后恢复同一状态。

人工减仓、AI平仓和Guardian止损共享持仓版本或事务锁。每次提交重新读取剩余数量，reduce_only必须由适配器和程序双重限制。重复价格事件不能重复退出；部分成交后只撤换剩余保护量。

同一K线触及多个价位的模拟执行遵循保守规则；实时保护使用时间有序的最新行情，不允许把开仓前的K线高低点当作开仓后的止损事实。

### A2. 独立行情和生命周期

行情订阅集合拆分为“策略观察品种”和“实际持仓保护品种”，底层订阅取并集并引用计数。暂停、删除策略或移除自选，只能释放策略订阅，不能释放持仓订阅。

| 用户动作 | 新机会扫描/AI开仓 | 已有委托处理 | 持仓保护 |
|---|---|---|---|
| 开始 | 授权和健康检查后启动 | 先对账历史未知委托 | 持续开启 |
| 暂停 | 停止新增风险，丢弃旧generation提案 | 对账在途委托，不假定已撤销 | 持续开启 |
| 恢复 | 新generation，更新行情与授权后恢复 | 先解决未决风险 | 持续开启 |
| 终止 | 终结策略会话，不自动恢复 | 按预先批准策略撤销未成交开仓单并对账 | 有仓位则继续 |

“终止策略”不等于“退出应用”或“立即清仓”。紧急平仓单独操作并显示确认结果。退出应用时明确说明本地保护是否会停止；实盘不得把依赖应用运行的本地止损冒充交易所原生保护。

独立心跳与行情新鲜度：分别记录worker_heartbeat_at、last_market_event_at、last_successful_reconcile_at。线程健康但行情过期必须显示DEGRADED并禁止新增风险；不能不断更新心跳掩盖数据断流。

### A3. 费用与经济事实

统一口径：gross_realized_pnl为不含费用的已实现盈亏；fee为独立费用；funding使用统一正负约定；net_realized_pnl = gross_realized_pnl - fees + funding。权益从该口径推导，严禁重复扣费。

至少登记account_id、venue、mode、order_id、trade_id、position_id、quantity、contract_size、price、fee_amount、fee_currency、fx_rate和event_at。非账户本位币费用需保留原币和可追溯转换率，缺失则标记待估值，不编造。

仓位变化、成交事件、费用事件和风险预算变化必须在可恢复事务中一致落地。相同venue/account/trade_id重复到达只入账一次。旧账本不能静默重算覆盖；先备份，能从成交重建的生成迁移明细，无法重建的标记LEGACY_UNVERIFIED并限制用于绩效。

## 5. 工作包B：统一执行、授权和硬风控

### B1. 禁止假成交和假保护

- RESEARCH不接受执行指令，返回MODE_NOT_EXECUTABLE。
- PAPER只能走SimulatedExecutionEngine；没有新鲜可执行价格返回MARKET_DATA_UNAVAILABLE，不使用68000或AI建议价格作为成交事实。
- TESTNET必须有账户绑定的可用测试网适配器；缺失返回EXECUTION_ADAPTER_UNAVAILABLE，不能生成成功订单。
- LIVE同时需要发布资格和有效用户授权；未满足继续拒绝。
- 保护计划创建为PENDING。只有明确的模拟保护登记或交易所保护单核验完成，才能标ACTIVE；保存一个stop_price不等于保护存在。

PAPER、TESTNET和LIVE使用相同委托/成交/账本契约，只有撮合与场所适配层不同。移除AI引擎自行写“假设已经成交”的逻辑。

### B2. 订单状态与对账

至少覆盖CREATED、RISK_APPROVED、SUBMITTING、ACKNOWLEDGED、PARTIALLY_FILLED、FILLED、CANCEL_PENDING、CANCELED、EXPIRED、REJECTED、UNKNOWN。ACK不是成交；部分成交后取消仍保留已成交数量和持仓。

请求超时但交易所可能收到时进入UNKNOWN；禁止盲目重提。按稳定client_order_id和场所订单ID查询，并拉取成交核对。适配器不支持查询时停止该账户新增风险并要求处理，不能把UNKNOWN自动当作REJECTED。

使用事务唯一键/原子claim解决并发幂等；相同键相同规范化载荷返回同一委托状态，不返回空字典；不同载荷409。载荷哈希采用规范化Decimal字符串和环境/账户身份，不固定8位截断。

### B3. 不可绕过的风控入口

所有AI、固定策略、手动接口、恢复任务均经同一网关。提交前重新验证授权状态/版本、账户环境、决策路径、模型摘要、策略版本、品种方向、数据时效、仓位版本、止损方向和完整参数。

统一检查单笔风险、组合风险、相关性组、日亏损、可用保证金、杠杆、频率、未决订单风险和保护健康。风险额度使用原v1.1定义及用户更严格限制，不在本轮擅自提高。

数量由程序根据可用风险预算、止损距离、合约乘数、手续费/滑点预算和交易规则计算。按数量步长向下取整；小于最小量则拒绝，不能用max(min_qty, qty)强行扩大风险。价格按tick和方向保守取整，不固定3/4位小数。

预算预占必须跨线程/进程原子执行，UNKNOWN和在途委托继续占用预算。取消确认、拒绝或结算后才能释放；进程重启从持久化预占恢复。

### B4. 授权及撤销竞态

授权记录保留account/venue/environment/control_mode/decision_path/model_digest/policy_version/instruments/directions/limits/expiry/version。客户端传confirmed_by字符串不能作为本地用户身份凭证；服务端根据受保护的本地确认流程产生确认事实，模型工具列表不暴露授权接口。

提交在进入适配器之前检查最新授权版本，并与撤销使用串行化提交边界。撤销完成后不得接受新开仓；已被场所接收的委托作为在途风险对账、尽力撤销，而不是保证网络世界中绝无后续成交。

撤销、过期、暂停和模型变更停止新增风险，但不阻断有效减仓与已有保护。减仓仍必须匹配账户、品种、剩余量及reduce_only，不能利用豁免反向开仓。

## 6. 工作包C：把千问自主决策接入产品

创建一个生产级会话协调器，复用现有运行时可用部分，不另开无法从桌面控制的独立演示循环。由它实例化AILedDecisionEngine、注入真实千问调用、收集快照、分配cycle_id并持久化周期结果。

模式来自已授权会话，不在引擎中写死PAPER。模型只能提出行动和理由，不能决定环境、账户或交易客户端。AI_LED与STRATEGY_DRIVEN使用不同decision_path和独立绩效归属。

保持v1.1周期和资源约束：默认5分钟，最短60秒，模型并发1，有界队列，每周期最多5品种；推理预算120秒，意图有效期60秒。推理前后都检查取消/generation/有效期，执行前再次检查行情和风险，过期结果仅记录不执行。

WAIT/HOLD、推理失败、结构拒绝、风控拒绝、提交、未知、成交都必须持久化，不能只保存成功单。周期包含输入快照引用、模型摘要、提示词版本、起止时间、实际延迟、action、reason_code、intent_id和结果。

动作完整性：OPEN_LONG/OPEN_SHORT经开仓风控；REDUCE_POSITION/CLOSE_POSITION经剩余量校验；TIGHTEN_STOP仅允许收紧，不扩大损失空间，更新保护必须避免先撤旧保护再装新保护造成裸露。WAIT/HOLD不创建交易委托。

进程崩溃后先取得会话租约、恢复订单/持仓/保护，再按用户预先配置决定是否恢复AI。双实例只能有一个执行持有者，失去租约的旧进程不能继续提交。

新闻与模型输出视为不可信输入；新闻文本中的指令不能调用工具、变更模式或申请授权。千问9B不可用时明确暂停AI动作，不静默换模型或用固定WAIT假装模型推理成功。

## 7. 工作包D：前端真实状态和接口整理

删除AITraderPanel的cycle_init_001、固定320ms和固定英文分析理由。初始状态为NOT_RUN；读取后端周期记录显示真实最后更新时间，区分加载、失败、陈旧和从未运行。

控制台显示：真实账户/环境、决策路径、授权范围及到期、会话状态、模型状态、最后行情时间、最后AI周期、最近拒绝原因、在途委托、持仓保护状态。开始/暂停/恢复/终止均调用服务端并等待确认，不能乐观显示“运行成功”。

账户来自后端账户注册表，不使用acc_gate_testnet_01/default_account等前端占位身份。统一API客户端、桌面backend地址、本地鉴权与错误处理，不绕过现有封装直接fetch相对地址。

建议最小接口契约，若已有同等接口则改造而非重复创建：

| 接口能力 | 输入/结果要点 |
|---|---|
| GET账户与能力 | 真实account_id、mode、venue、能力来源和检查时间 |
| POST会话及操作 | authorization_id、expected_version；返回session/generation/state |
| GET会话状态/周期 | cycle记录分页、最新行情与模型健康、持仓保护摘要 |
| POST委托 | 显式account_id和幂等键；环境从账户核对，拒绝错配 |
| GET委托/成交 | 状态、累计成交、未决数量、保护状态及对账时间 |
| GET账本/持仓 | 同一账户快照版本和截至时间 |
| 授权确认与撤销 | 受保护的本地确认流程，服务端记录确认身份与版本 |

接口失败统一返回error_code、可读消息、request_id和可否重试；不得泄露密钥。中文/英文均使用翻译资源，不在新组件硬编码整段英文。诊断导出不能未经验证显示“Zero Leaks Verified”。

## 8. 工作包E：真实TESTNET能力与证据

TestnetCapabilityService从账户绑定的适配器读取能力和当前品种规则；每项能力返回AVAILABLE/UNSUPPORTED/UNKNOWN、来源、检查时间和有效期。静态配置只能作为声明，不能充当协议探测证据。

原生止损验收需取得实际保护订单ID、数量、触发价格、状态，并验证与真实测试持仓相匹配。费用来自实际成交，资金费来自实际流水；预估计算另标ESTIMATED，缺失 reported_fee 不得自动判断匹配。

未获测试网账户授权或网络不可用时标NOT_RUN/BLOCKED_BY_ENVIRONMENT，不伪造成功。测试网不支持能力标UNSUPPORTED；本地模拟保护不能冒充原生保护或作为生产资格证据。

manifest默认全部NOT_RUN。读取pytest JUnit、前端报告、构建日志和运行记录生成状态；每个验收项绑定测试层级、报告路径/哈希、时间、退出码、代码快照、环境与断言范围。不存在报告或只有mock时不得标真实环境PASS。

记录dirty补丁摘要/哈希、关键源码和安装产物SHA256。旧证据不删除，但标明已被新版本替代。72小时加速循环只叫模拟压力测试，不得标72小时稳定运行。AT41无资金的预检可验证；AT42真实资金流程仍等待独立授权。

## 9. 专项验收矩阵（新增RT编号，不覆盖原AT编号）

| 编号 | 场景与必须断言 | 层级 |
|---|---|---|
| RT01 | TP1后再穿止损，剩余量归零，退出事件唯一 | 集成 |
| RT02 | TP1后TP2、人工减仓后止损、部分平仓后重启均正常 | 集成 |
| RT03 | 从真实运行入口暂停，再由行情适配器输入新事件触发保护；不得直接调用Guardian | 集成 |
| RT04 | 删除订阅/终止策略不删除持仓保护行情 | 集成 |
| RT05 | 线程心跳正常但行情断流，状态降级并拒绝开仓 | 故障注入 |
| RT06 | AI与Guardian并发平仓，不超卖、不重复入账 | 并发 |
| RT07 | 毛利润10、费用0.11，权益增加9.89；亏损/资金费同样精确 | 集成 |
| RT08 | 重复成交、部分成交多次到达、重启重放，账本一致 | 集成 |
| RT09 | PAPER无行情拒绝；不同品种按各自行情撮合 | API集成 |
| RT10 | 无TESTNET适配器明确失败，不产生ACK/假订单号 | API集成 |
| RT11 | open+部分filled、取消前部分成交、超时UNKNOWN状态正确 | 适配器契约 |
| RT12 | 保护订单失败/状态未知禁止新增风险，不能显示ACTIVE | 集成 |
| RT13 | 直接API、AI和策略提交都执行同一硬风控 | API集成 |
| RT14 | 并发预占不突破组合预算；未知委托不释放预算 | 并发 |
| RT15 | 授权到期/撤销/模型或环境错配拒绝；合法保护减仓仍允许 | 集成 |
| RT16 | 亚分价格、不同合约乘数和步长，数量不向上扩大风险 | 参数化 |
| RT17 | 界面开始会话后实际调用千问，连续两周期产生可追踪记录 | 桌面端到端 |
| RT18 | 没有固定策略触发时AI_LED仍可提出有效模拟交易 | 端到端 |
| RT19 | 模型超时/返回过期/旧generation不下单；界面显示真实原因 | 故障注入 |
| RT20 | AI平仓与收紧止损均实际反映到订单/保护，不仅更新文本 | 集成 |
| RT21 | 双应用实例和重启只保留一个会话执行者 | 进程集成 |
| RT22 | 未运行、运行失败、数据陈旧不会显示伪造AI周期 | 前端+桌面 |
| RT23 | 真实TESTNET成交/保护ID/费用与账户流水一致 | 外部环境 |
| RT24 | 未执行的验收项生成NOT_RUN；删除报告后不能继续PASS | 证据工具 |
| RT25 | 全新Windows安装无开发环境，启动、升级、卸载与旧数据迁移可核验 | 干净环境 |
| RT26 | 原六策略/新闻/翻译/回测/只读研究功能回归不退化 | 回归 |

RT17真实模型不可用时允许先跑受控假模型的集成测试，但该结果不可代替真实模型端到端记录。RT23和RT25没有条件执行时保留未通过发布项，不用降低断言解决。

## 10. 分阶段交付与停止条件

### P0：冻结基线与安全封口

记录工作区快照；为F01—F07建立失败测试；关闭无适配器成功回执与静态运行展示。检查所有下单入口，维持实盘锁。交付：问题映射、失败复现、待验证列表。禁止为了制造失败测试修改生产账户。

### P1：统一账本、执行和持仓保护

一次性完成工作包A、B；先使RT01—RT16通过。同步处理旧路径双重撮合/双重记账，必要时将旧入口改为调用统一链路，确认无调用后再移除重复实现。验收不通过不得进入真实场所试运行。

### P2：千问真实接线与桌面体验

一次性完成工作包C、D，RT17—RT22通过。交付一个实际能连续运行、暂停后仍保护持仓、可以回放决策到成交的PAPER版本。缺少真实模型证据时不能声称AI自主运行已验收。

### P3：发布证据和授权TESTNET

完成工作包E、RT23—RT26及原AT回归。构建安装包并在干净Windows验证。按外部条件分别交付“PAPER可验收”“TESTNET已验证”“LIVE仍锁定”的准确状态，禁止笼统宣布所有开发完成。

### P4：独立实盘资格审查

不属于本方案的自动解锁步骤。只有前置修复和实际环境证据齐备后，另行确认账户、场所、允许品种、限额、期限及紧急处置，才可开展有界真实验证。不承诺盈利，不以测试通过代表策略存在收益优势。

协作约束：每阶段作为完整工作包交给同一开发者，保留上下文；阶段内自行集中修复测试失败，不反复拆成微任务。主管只在完整交付或明确阻塞时审阅，不循环读取仓库、不反复发送“等待”，不自动中断长任务。最终问题一次汇总，下一轮整包修复。

## 11. 必交付产物与完成定义

1. 对应修复代码、迁移脚本和回滚说明；不要只交独立工具类。
2. F01—F07 → 修改文件 → RT/AT用例 → 报告的追踪表。
3. API/数据状态契约，尤其授权、订单、持仓、保护、账本字段。
4. 原有回归及新测试机器可读报告，标清mock/真实模型/真实场所/干净安装层级。
5. 完整PAPER会话证据：界面启动→模型周期→风控→委托→成交→保护退出→账本权益。
6. 真实TESTNET证据（如已获授权执行）；否则明确缺失条件。
7. 安装包及哈希、启动/暂停/恢复/终止操作说明、已知限制。
8. 新manifest：默认未运行，不硬编码通过，不漏掉新增测试文件和运行日志。

完成定义：普通用户从安装包操作真实界面，可以辨明哪些功能正在运行、哪些失败或未配置；AI记录来自真实推理，订单来自明确执行环境，成交与账本可对账，暂停策略不会让剩余仓位失去保护。任何一项仅靠占位数据或人工绕过入口才能演示，均不算完成。

## 12. 本轮不新增的内容

暂不引入多模型投票、策略市场、跟单排行、云端多账户、多交易所全面扩张、AI自动调高杠杆等功能。新闻可信度和六策略维持v1.1原合同并回归；本方案没有宣称它们已完成全面复审。先完成可靠性和可核验性，再依据真实运行数据决定下一轮功能。
