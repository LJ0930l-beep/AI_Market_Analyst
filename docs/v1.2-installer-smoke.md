# V1.2 Windows installer and lifecycle smoke

Date: 2026-08-31
Installer: `D:\RJ\codex\ai-market-analyst\src-tauri\target\x86_64-pc-windows-gnu\release\bundle\nsis\AI Market Analyst_1.2.0_x64-setup.exe`
Installer SHA-256: `97FA0CF1778F423AEE12E486C818BB7FB38730A9E300A796591DC5A8BFD698D4`
Installer size: `42,531,412` bytes
Release Tauri executable SHA-256: `C06FE7A75FDD36169F2F0B928A5B3AC65E76CC93DE9443F09FE8AAA29CDA7321`
Release packaged sidecar SHA-256: `64D416AFCFBBC21B91490B7A94B84660218AE06777038F2536BBC5F8682FD84B`
Default program directory: `%LOCALAPPDATA%\Programs\AI Market Analyst`
Runtime data directory: `%LOCALAPPDATA%\AI Market Analyst`
Isolated smoke data directory: `C:\Users\baicha\AppData\Local\Temp\aima-v12-repair-final\appdata`
Isolated smoke port: `18771`

## Reproducible build command

From `D:\RJ\codex\ai-market-analyst\src-tauri` in a clean checkout at the amended V1.2 commit:

```powershell
cargo +stable-x86_64-pc-windows-gnu tauri build --target x86_64-pc-windows-gnu
```

The command passed end-to-end. Tauri invoked the repository-root `scripts/build-tauri.ps1`; that wrapper resolves its child scripts from `$PSScriptRoot`, builds `web/dist`, builds the PyInstaller sidecar, verifies non-zero child exits, and then produced the Windows x64 NSIS installer. The installed package contains built React assets and a packaged sidecar and does not require Vite, npm or Node at runtime.

## Installed lifecycle

- The final NSIS artifact was installed with `/S`, exit code `0`, and created the normal current-user program directory. A second install of the same final artifact also returned exit code `0`.
- Launching the installed executable with `AIMA_SIDECAR_PORT=18771`, `AIMA_DATA_ROOT` set to the isolated path above and `ALLOW_FIXTURE_FALLBACK=0` opened the real Tauri window `AI Market Analyst · V1.2`. `/health` returned `status=ok`, `phase=7`, `api_version=1.2.0`, `real_orders=false` and `private_keys=false`.
- On a clean relaunch, `/monitoring/status` was `state=stopped`, `active=false`, `worker_alive=false`, `run_count=0`, `resume_eligible=false`; Auto Start was absent from the Windows Run key. Persisted enabled policies do not start the runtime and are not scanned on startup.
- The installed sidecar runtime was exercised through its actual API: stopped/run_count 0 -> start/active -> completed cycle/run_count 1 -> pause/inactive with a stable count -> resume/active -> completed cycle/run_count 2 -> stop/stopped with no worker. This was the final rebuilt package, not a fixture claim.
- The active-window close path was exercised while the runtime was active: the app stayed alive, the owned sidecar and port stayed alive, and the one-time monitoring-continues notice path was emitted. With monitoring inactive, `close_to_tray=true` kept the app in the tray; with `close_to_tray=false`, X exited and released the owned sidecar/port. Tray Exit uses the same exact-owned shutdown path.
- The final package's tray contains Show, Open monitoring terminal, dynamic Monitoring state, Pause/Resume, Settings routed to `/settings`, Restart owned backend and Exit. The source/security tests assert the menu wiring; the Windows automation surface exposes the app window but not the native tray popup, so individual dynamic tray clicks are not claimed as directly observed here.
- A deliberate backend crash of the exact packaged sidecar produced the desktop degraded state; the Settings-page `Restart owned backend` action then restored `backend ready` without touching foreign processes. A default-port conflict with the unrelated Codex sidecar kept the desktop alive/degraded and left the foreign listener intact.
- A second launch returned exit code `0` and did not create a second app window; single-instance focus behavior was retained. The exact app/sidecar process records and loopback listener were the only product processes targeted.

## Windows login autostart and uninstall retention

- Settings enabled the official Tauri autostart integration. The observed HKCU Run value was named `AI Market Analyst` and pointed to the installed executable; the matching `StartupApproved\Run` value was present. Resume Monitoring remained an independent preference and was not enabled by Auto Start.
- The final uninstall was run with `/S`, exit code `0`, while the autostart value was enabled. It removed the program directory and both exact `Run` and `StartupApproved\Run` values. With the app closed, the SQLite database remained byte-for-byte (`733,184` bytes, SHA-256 `DD6D1B14CF90FB11432D61B08BF4C87B22D701B0BC4C25B7A3F2C0815E057F0F`) and Ollama PID `8320` remained alive.
- The final artifact was installed again after that uninstall and launched successfully with Monitoring stopped and Auto Start off. Uninstall/upgrade therefore preserves data while removing invalid login-registration residue.

## Notifications and honest observation boundary

- The installed Settings action successfully requested a Windows native test notification and displayed the in-app confirmation. The installed notification smoke observed Windows PushNotification event-log delivery. The always-mounted desktop bridge consumes strict `alert.created` SSE events, deduplicates event IDs and routes safe Alert Center/asset paths while the window is hidden; native permission failure remains an in-app degradation.
- This Windows automation surface did not retain a durable visual toast or directly observe a normal Windows toast click callback. The code and browser tests cover the safe Tauri action route, but this document does not claim direct OS click observation as passed. See the completion report for the platform limitation and fallback behavior.

## Interpretation

This is a real installed Windows smoke, not a fixture claim. The separate [v1.2-live-smoke.json](v1.2-live-smoke.json) records fixture fallback disabled, public Binance REST/WS for BTCUSDT/ETHUSDT/SOLUSDT, 15m/1h chart data, the resident runtime lifecycle, real qwen3.5:9b OpportunityAnalysis and real qwen3.5:4b news translation with numeric guard. The installer installs no Ollama and requires no paid service, exchange account, secret or API key. Uninstall removes program files and login residue only; AppData data, logs, backups and runtime evidence remain for recovery or explicit operator cleanup.
