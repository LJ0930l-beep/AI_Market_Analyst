# V1.2.1 Installed Windows Smoke

Date: 2026-09-03
Host: Windows 11 x64, current-user NSIS install, formal default AppData environment.

## Package

- Installer: `D:\RJ\codex\ai-market-analyst\src-tauri\target\x86_64-pc-windows-gnu\release\bundle\nsis\AI Market Analyst_1.2.1_x64-setup.exe`
- Size: `42,553,612` bytes
- SHA-256: `B94D80529BA4D49CE5FA3F2F2AA0FB9C3466915AE62D8257939D83216F2FD360`
- Installed executable: `C:\Users\baicha\AppData\Local\Programs\AI Market Analyst\ai-market-analyst.exe`, file version `1.2.1`.
- Data: `C:\Users\baicha\AppData\Local\AI Market Analyst\data\market_analyst.sqlite3`, schema `13`.

## Observed lifecycle

1. Install/launch reached the packaged backend and hydrated the desktop. A second launch retained one product window/process owner and activated the existing instance.
2. Auto Start was initially disabled. Enabling it in Settings created `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\AI Market Analyst` with value `C:\Users\baicha\AppData\Local\Programs\AI Market Analyst\ai-market-analyst.exe`. Disabling it removed the Run value. Resume Monitoring remained independently false.
3. Only the exact verified owned sidecar tree was terminated to simulate a crash. The UI entered backend-degraded state; Restart owned backend created a new owned sidecar and restored health. Ollama remained alive.
4. A controlled unrelated Python listener retained `127.0.0.1:18765`. The application left it untouched, selected `18766`, reached backend-ready and then exited without affecting that listener.
5. Monitoring was explicitly started. The runtime reported active/worker true, connected public WS and completed background cycles. Closing X kept the app/sidecar alive while hidden and displayed the one-time “monitoring continues in the background” notice when reopened. Explicit stop ended work and cleared resume eligibility.
6. With Monitoring stopped and close-to-tray false, X exited the product and released its loopback listener. Relaunch restored health. Only tray Exit or an explicit inactive close terminates the owned sidecar.
7. Before uninstall, AppData database size was `1,437,696` bytes and SHA-256 was `C79A2B53BC480FC60DE26C84123E10A1853B4931E89F2F9CD29C0A08D02D44CE`. Silent uninstall returned `0`, removed the program directory plus Run/StartupApproved entries, and left the database byte-for-byte unchanged. The final acceptance installer upgrade also returned `0`; its immediate pre/post database was `1,695,744` bytes with unchanged SHA-256 `8F8AFB8D6C19B3A9ED539CA8089C171C8B7C5F019AB694026E2CB9E6B6560280`.
8. Final defaults/settings are `desktop.auto_start=false`, `monitoring.resume=false`, `desktop.close_to_tray=false`; runtime was explicitly stopped, resume eligibility cleared, the application exited, and all listeners in `18765..18828` were released. Final installed executable SHA-256 is `1FD7A9EA56F6F8D13841BB5981316F9ACD13FE18D7001DBFF0CAE60CB3B5F571`. Ollama was not stopped during any lifecycle operation.

## Final installed live UI evidence

The final package was installed silently with exit code `0`, then launched normally with no environment override. Its owned backend selected `http://127.0.0.1:18765`; no browser dev server, fixture, mock or test port participated. Direct Tauri-window captures observed:

- Dashboard: fresh real `binance_public` BTC/ETH/SOL market rows, explicitly stale/degraded `yfinance` rows, and available stored real news/calendar evidence.
- Settings: backend ready, API `1.2.1`, schema `13`, actual port/PID/instance, `desktop_backend_v1`, ownership verified, and real provider routes.
- Monitoring: 240 BTCUSDT 15m bars from `binance_public`; after explicit start, runtime `running`, worker alive, public WS connected, one enabled symbol and three completed REST cycles.
- Signals: zero Prediction rows plus deterministic BTC/ETH/SOL market/freshness evidence, `trigger_policy_v2` state and explicit per-symbol analysis links. Page load made no model request.

Capture timestamps, instance identifiers and screenshot SHA-256 values are in `docs/v1.2.1-installed-live-smoke.json`; the human-readable split between live-installed and deterministic fixture images is in `docs/v1.2.1-screenshot-inventory.md`.

## Notification and tray observation boundary

The installed Settings action successfully requested a Windows native notification and displayed the product confirmation. Background alerts are bridged by an always-mounted listener after backend dedupe/cooldown and retain Alert Center/in-app deep-link routing if permission is denied. This automation surface did not expose a durable ordinary Windows toast click, so direct toast-click activation is not marked observed.

The system taskbar tray popup was not exposed as a targetable automation window, and prohibited Windows-key navigation was not used. Tray source/tests prove live status, pause/resume, Settings, backend restart and Exit wiring; installed active-close/background-worker/reopen behavior provides real lifecycle evidence. A direct tray popup click sequence is not claimed.

## Safety result

No test created an exchange account, secret, key, broker or order. The controlled foreign listener and Ollama survived all application restart/exit/uninstall checks. The app acts only on its exact owned sidecar process identity.
